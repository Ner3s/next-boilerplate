import { {{pascalCase name}}Template } from '.';

import { screen, render } from '@/utils/test';

describe('<{{pascalCase name}}Template />', () => {
  it('should render {{pascalCase name}}Template', () => {
    render(<{{pascalCase name}}Template />);

    expect(
      screen.getByRole('heading', {
        name: /{{pascalCase name}}Template/i
      })
    ).toBeInTheDocument();
  });
});
